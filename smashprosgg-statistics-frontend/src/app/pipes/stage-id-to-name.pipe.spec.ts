import { StageIdToNamePipe } from './stage-id-to-name.pipe';

describe('StageIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new StageIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
